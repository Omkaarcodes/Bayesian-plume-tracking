# Bayesian-plume-tracking
Simulates a gas leak in a confined space using a finite-difference solver for the 2D diffusion PDE, then localizes the source via Bayesian inference over noisy sensor readings, guided by an A*-based agent that plans toward the most informative regions.
